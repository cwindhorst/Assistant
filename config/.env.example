# Example ~/.hermes/.env
#
# REFERENCE file — copy into ~/.hermes/.env on the Linux machine and fill in real values.
# NEVER commit a real .env with actual secrets to this repo.

# xAI (Grok) provider
XAI_API_KEY=sk-xai-REPLACE_ME

# Telegram bot token, from @BotFather
TELEGRAM_BOT_TOKEN=REPLACE_ME
